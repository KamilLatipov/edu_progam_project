mixin rich-checkbox( { title, items} )
  each item in items
    .rich-checkbox
      label.rich-checkbox__label= item.text
        input( type="checkbox" )
        span.rich-checkbox__checkmark
      p.rich-checkbox__subtext= item.subtext