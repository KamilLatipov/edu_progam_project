mixin type
  ul.type__list
    li.type__item
      .type__tag.type__h1 h1
      h1.type__example.type__h1 This one is the sub-section or widget title
    li.type__item
      .type__tag.type__h2  h2
      h2.type__example.type__h2 Next one is the item title inside widgets
    li.type__item
      .type__tag.type__h3 h3
      h3.type__example.type__h3 This is a label or CTA text
    li.type__item
      .type__tag.type__body Body
      p.type__example This is the body text which is used for most of the design, like paragraphs, lists, etc.
